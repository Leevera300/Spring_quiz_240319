<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.lesson03.mapper.RealEstateMapper">
 	<select id="selectRealEstateById" resultType="com.quiz.lesson03.domain.RealEstate">
 		SELECT
 			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM
			`real_estate`
		WHERE
			`id` = #{id}
 	</select>
 	
 	<select id="selectRealEstateByRentPrice" resultType="com.quiz.lesson03.domain.RealEstate">
 		SELECT
 			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM
			`real_estate`
		WHERE
			<![CDATA[
        	`rentPrice` < #{rentPrice}
        	]]>
 	</select>
 	
 	<select id="selectRealEstateByAreaAndPrice" resultType="com.quiz.lesson03.domain.RealEstate">
 		SELECT
 			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM
			`real_estate`
		WHERE
			<![CDATA[
        	`area` >= #{arg0}
        	AND
        	`price` <= #{arg1}
        	AND
        	`type` = '매매'
        	]]>
 	</select>
 	
 	<insert id="insertRealEstate" parameterType="com.quiz.lesson03.domain.RealEstate">
 		INSERT INTO `real_estate`
 			(
 			`realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `createdAt`
			, `updatedAt`
 			)
 		Values
 		(
 			#{realtorId}
 			, #{address}
 			, #{area}
 			, #{type}
 			, #{price}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 	
 	<insert id="insertRealEstatebyRealtorIDandAsField" parameterType="map">
 		INSERT INTO `real_estate`
 			(
 			`realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
 			)
 		Values
 		(
 			#{realtor_id}
 			, #{address}
 			, #{area}
 			, #{type}
 			, #{price}
 			, #{rentPrice}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 </mapper>